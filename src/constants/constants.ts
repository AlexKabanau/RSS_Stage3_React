export const URL = {
  baseUrl: 'https://swapi.dev/api/',
  props: 'starships/',
  search: '?search=',
  page: '&page=',
  limit: 'limit',
};

export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_PAGE = 1;
